{
  "name": "js",
  "version": "1.0.0",
  "description": "",
  "main": "search_test.js",
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "cln-build": "npm run clean && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/nouislider": "^9.0.10",
    "nouislider": "^15.8.1"
  }
}
